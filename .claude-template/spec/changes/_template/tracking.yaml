id: {{id}}
title: "{{title}}"
description: "{{description}}"
status: backlog
scope: ""
stages:
  refinement:     pending
  design:         pending
  decomposition:  pending
  implementation: pending
  verification:   pending
history:
  - { at: "{{now}}", stage: lifecycle, status: created, by: user }
