schema: spec-driven

# Project-wide context injected into all spec artifacts. Max 50 KB.
# Use this to constrain AI choices project-wide
# (e.g. "use the Repository pattern for DB access", "no shared mutable singletons").
context: |

# Per-artifact rules. Each list item is one constraint
# the AI must honour when generating that artifact.
rules:
  proposal: []
  spec: []
  design: []
  tasks: []
