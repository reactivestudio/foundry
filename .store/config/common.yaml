# Настройки, которые читают оба фронтенда — CLI и приложение.
# Пока таких нет: всё, что есть сегодня, относится только к CLI (cli.yaml).
# Настройка едет сюда тогда, когда её спрашивают двое; одному нужна — значит
# она не общая.
#
# Проектные переопределения — projects/<id>/config.yaml, они побеждают.
